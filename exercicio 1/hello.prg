/*/{Protheus.doc} Main
)sc @type     Function
    @aut    hor user
    @since 12/10/2022
    @version version
    @pa 
    r ""
    Waita "Pressione uma tela para finalizar"m param_name, param_type, param_descr
    @return return_var, return_type, return_description
    @example
    (examples)
    @see (links_or_references)
    /*/
Function Main()  
    ? ""
    ? "Hello world"
    ? ""

    Wait "Pressione uma tela para finalizar"
RETURN nil
